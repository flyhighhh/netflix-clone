import React, {useState} from 'react'

//컴포넌트 파일은 파일명 대문자로

function Row({ title }) {
    const [movies, setMovies] = useState([]);
    
    //

  return (
    <div>
        <h2>{title}</h2>

        {/* container -> posters */}
        
    </div>
  )
}

export default Row